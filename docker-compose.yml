version: '3.7'

services:
  web:
    build:
      context: ./web/
      dockerfile: Dockerfile
    command: gunicorn main:app -b 0.0.0.0:8000
    volumes:
      - static_volume:/home/app/web/static
      - media_volume:/home/app/web/media
    expose:
      - 8000
    depends_on:
      - redis
    networks:
      - private

  redis:
    image: redis
    volumes:
      - redis_data:/var/lib/redis/data/
    networks:
      - private

  nginx:
    build: ./nginx
    volumes:
      - static_volume:/home/app/web/static
      - media_volume:/home/app/web/media
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.web_chifuri.entrypoints=http"
      - "traefik.http.routers.web_chifuri.rule=Host(`groot.ninja`)"
      - "traefik.http.middlewares.web_chifuri-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.routers.web_chifuri.middlewares=web_chifuri-https-redirect"
      - "traefik.http.routers.web_chifuri-secure.entrypoints=https"
      - "traefik.http.routers.web_chifuri-secure.rule=Host(`groot.ninja`)"
      - "traefik.http.routers.web_chifuri-secure.tls=true"
      - "traefik.http.routers.web_chifuri-secure.tls.certresolver=http"
      - "traefik.http.routers.web_chifuri-secure.service=nginx-groot@docker"
      - "traefik.docker.network=traefik-proxy"
    depends_on:
      - web
    networks:
      - private
      - public
      - traefik-proxy

networks:
  private:
    driver: overlay
  public:
    external: true
  traefik-proxy:
    external: true

volumes:
  redis_data:
  static_volume:
  media_volume:
